#!/usr/bin/python3

def divisible_by_2(ints=[]):
    log = []

    for i in range(0, len(ints))
        if ints[i] % 2 == 0:
            log.append(True)
        else:
            log.append(False)
    return log
