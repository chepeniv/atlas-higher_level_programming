#!/usr/bin/node

// select and update the <header> tag's color to #ff0000
// use document.querySelector
// cannot use the jquery api
const header = document.querySelector('header');
header.style.color = '#ff0000';
